/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['img.freepik.com', 'i.pinimg.com', 'thumbs.dreamstime.com', 'encrypted-tbn0.gstatic.com','avatar.github.com']// Add your domain if images are hosted externally
      },
};

export default nextConfig;
